# Стратег для маркетплейсов

Простой рабочий MVP AI-платформы для продавцов маркетплейсов: загружаете выгрузки Ozon/Wildberries, сервис нормализует отчёты, считает метрики, учитывает юнит-экономику и формирует стратегию роста продаж на месяц.

## Что есть

- Frontend: React + TypeScript + Vite.
- Backend: Express API.
- Данные хранятся только в памяти запущенного сервера.
- Серверная загрузка CSV, XLSX, XLS и ODS.
- Мультизагрузка: можно отправить сразу несколько отчётов и получить единый анализ по SKU.
- Готовые пресеты под выгрузки Ozon/Wildberries.
- Нормализация типов отчётов:
  - продажи;
  - реклама;
  - остатки;
  - акции/промо.
- Справочник юнит-экономики по SKU:
  - себестоимость;
  - комиссия;
  - эквайринг;
  - логистика;
  - налог.
- Импорт справочника юнит-экономики из XLSX/CSV/ODS.
- Метрики:
  - продажи;
  - заказы;
  - ДДР;
  - маржа;
  - остатки;
  - расходы по юнит-экономике;
  - показы;
  - клики;
  - корзины;
  - конверсии показ→клик, клик→корзина, корзина→заказ.
- Диагностика качества данных: распознанные отчёты, недостающие отчёты, предупреждения и подсказки перед стратегией.
- Встроенная стратегия по правилам.
- AI-генерация стратегии при наличии `OPENAI_API_KEY`.
- Экспорт бренд-отчёта в XLSX и PDF.

## Запуск

```bash
npm install
npm run dev:server
npm run dev
```

API работает на `http://localhost:8787`.

## Настройки `.env`

Если нужен AI-анализ через OpenAI, создайте в корне проекта файл `.env` рядом с `package.json`.

Пример `.env`:

```bash
PORT=8787
OPENAI_API_KEY=ваш_ключ_openai
OPENAI_MODEL=gpt-4o-mini
```

Что это значит:

- `PORT` — порт backend-сервера. Обычно можно оставить `8787`.
- `OPENAI_API_KEY` — ключ OpenAI. Если ключа нет, строку можно не добавлять.
- `OPENAI_MODEL` — модель для AI-стратегии. Можно оставить `gpt-4o-mini`.

Если `OPENAI_API_KEY` не указан, сервис всё равно работает и использует встроенную стратегию по правилам.

